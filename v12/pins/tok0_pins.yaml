# TOK-0 pin file — v12 tokenizer design funnel, DRAFT v0.5
# Source: v12-tokenizer-design-funnel.md, owner Chris (CHUK)
# Tracked: chuk-experiments programme "v12-tokenizer", experiment "tok-0-harness-pinning"
#
# Moved 2026-07-19 from tiny-model/tokenizer/v12/pins/ into the standalone
# v-tokenizers repo (chris-source/v-tokenizers/v12/pins/) -- fresh git
# history, no history carried over. All in-repo path references below are
# relative to the new v-tokenizers root; absolute external paths (cell80,
# chris-experiments) are untouched.
#
# Two kinds of entries below:
#   - commitments: frozen in the design doc itself (C0-C10), reproduced here verbatim
#   - open pins: marked `null  # PIN: not yet decided` — a research decision, not a default

commitments:
  C0_single_decision: "tokenizer chosen once for the 1B endpoint, validated (never re-optimized) at 115M"
  C1_tied_embeddings: true
  C2_eval_stream:
    description: "atlas stream: 24M tokens (16M seed42 phase-1 + 8M seed43 phase-3)"
    max_seq: 256
    status: "FOUND 2026-07-19 — real, already built. Referenced in place (external repo), not copied."
    location_repo: "/Users/christopherhay/chris-source/cell80/experiments/corpus-atlas"
    source_dataset: {name: "roneneldan/TinyStories", split: "train", streaming: true, shuffle_buffer: 10000, hub_sha: "f54c09fd23315a6f9c86f9dc80f725de7d8f9c64", hub_last_modified: "2024-08-12T13:27:26Z"}
    tokenizer_used_for_this_dump: {path: "chris-experiments/compilation/15_v11_model/v11_tokenizer/v11.model", sha256: "4ffbfc8713db306a4b155f54c828002492527046fa567a2ef35eeb277c1dc8e6", vocab_size: 71261}
    phase1: {file: "artifacts/indices/v11_stream_phase1_run1.u32", seed: 42, tokens: 16000000, dtype: "uint32-le", sha256: "883b483c57baaaedbf6951b145b6e7c0907d85dd86ab81393882f5fef0bebbfb", distinct_ids: 15758, stories_consumed: 60191}
    phase3: {file: "artifacts/indices/v11_stream_phase3_run1.u32", seed: 43, tokens: 8000000, dtype: "uint32-le", sha256: "bd0dd555f3b29cd6aacfc0b26aa901ebbc395e73ec2daa8175a9f7791adb3efd", distinct_ids: 13207, stories_consumed: 30452}
    manifest: "artifacts/a0_manifest_run1.json (+ run2, re-derivation check: phase1 sha matches bit-for-bit)"
    per_candidate_retokenize_tool: "a0_dump_stream.py — the 'a0 machinery per tokenizer' this commitment requires; re-tokenizes the same pinned raw TinyStories stream (same hub_sha/seed/shuffle_buffer) under any candidate tokenizer"
    caveat: "this is the v11-tokenized DUMP of the stream, not a separately cached raw-text file — raw text is reproduced deterministically by re-streaming the pinned hub revision with the pinned seed/shuffle_buffer; distinct from what C8 needs (C8 is the TRAINING corpus, this is the EVAL stream)"
  C3_held_out_slice:
    description: "blind, frozen, pre-freeze exposure appendix required"
    status: "FOUND 2026-07-19 — referenced in place, not copied. NOT unconditionally reusable, see caveat."
    location_repo: "/Users/christopherhay/chris-source/cell80/experiments/corpus-atlas"
    file: "div0_heldout.json"
    exposure_appendix: "div0_heldout_prefreeze_exposure.md"
    frozen: "2026-07-17"
    seed: 91
    n_items: 40
    corpus_sha256_checked_against: "4e146e6de2223a771f584c5ebfe0beec05fd6c9709378849b7cfc24d4472712c"  # == cn7_corpus_train.jsonl sha, i.e. disjointness was verified against the CN-7 corpus specifically, not the TinyStories atlas stream
    caveat: >
      Its own rule field: "this set is scored ONCE, after selection... a failed confirmation is a
      result about the instrument, not a license to reselect." This is a spent/single-use
      confirmation instrument for the corpus-atlas/DIV-0 protocol, not a generic reusable held-out
      set. v12 should treat this as a REFERENCE PATTERN (same disjointness + prefreeze-exposure
      discipline) and draw its own fresh C3 slice when the real C2/C8 pipeline runs for v12,
      rather than reusing these 40 items outright — flagged for Chris to confirm.
  C4_one_trunk:
    active_params_only: true
    dormant_excluded_from: ["projection_matmul", "optimizer_state"]
  C5_structural_refusal: "every artifact carries tokenizer sha256 + stream sha256 (+ corpus shas); refuses hash-mismatched input"
  C6_digit_granularity: "single-char digits; atomic multi-digit priced once as ABL-1"
  C7_dormant_blocks_immutable: "string/id/normalization/merge-precedence/activation-stage/encoding-eligibility frozen before TOK-0, no later additions"
  C8_tokenizer_training_corpus:
    description: "superset (TinyStories + code + math + structured), frozen: datasets+revisions, per-domain byte counts, mixture proportions, sampling seed, normalization, dedup, C3-slice exclusion, repeated-identifier cap"
    status: "3 of 4 domains have real source material (found 2026-07-19); no unified manifest existed until v12/c8_manifest.json (this session). Mixture proportions there are PROVISIONAL v0 -- reasonable defaults to unblock experimentation, not a frozen research decision."
    domain_prose:
      source: "roneneldan/TinyStories (same pinned hub_sha as C2 eval stream: f54c09fd23315a6f9c86f9dc80f725de7d8f9c64)"
      status: "trivially available — streamed, no local storage needed"
    domain_math_structured:
      status: "FOUND 2026-07-19 — real, sha-pinned, referenced in place (not copied)"
      location_repo: "/Users/christopherhay/chris-source/cell80/experiments/cell-native-architectures/artifacts/datasets"
      files:
        - {file: "cn7_corpus_train.jsonl", rows: 140154, bytes: 98360277, sha256: "4e146e6de2223a771f584c5ebfe0beec05fd6c9709378849b7cfc24d4472712c"}
        - {file: "cn8_corpus_b.jsonl", rows: 51031, bytes: 54224484, sha256: "f582eb612468ee591c02df8a0c475a253a38e0397fc71c34b1c847494873a8d9"}
        - {file: "cn8_corpus_atok.jsonl", rows: 346962, bytes: 74395145, sha256: "6d1401f6d4e161e3401eb482a1bb78ed1723b24513a663b6c8a9505339a36206"}
        - {file: "cn8_corpus_aex.jsonl", rows: 51031, bytes: 10942232, sha256: "a7a52b74b0f836a4170fdb77ce4888f1b776808f822de858eb60aac4b604bef2"}
      content: "arithmetic word problems + <call>...</call> cell-grammar syntax, already single-char-digit tokenized (matches C6); each row has text/ids/loss/meta fields"
      caveat: "built for the CN-7/CN-8 cell-native-architectures experiments, not originally for v12 tokenizer training — reused here as real, honestly-sourced math+structured text, not purpose-built for C8"
    domain_code:
      status: "did not exist anywhere (checked cell80 + tiny-model datasets/ + AST-metadata dirs) -- built fresh this session"
      location: "v12/corpus/c8_code_corpus.jsonl (this repo, gitignored -- regenerate via corpus/build_code_corpus.py or pull the artifact from chuk-experiments)"
      source: "real source files harvested from this repo's own Rust/Python codebase — see manifest for exact file list + shas"
      caveat: "small (few MB) relative to the math/prose domains — a v0 seed sample, not a comprehensive code corpus; no external code dataset was pulled in"
    manifest: "v12/corpus/c8_manifest.json — the actual frozen manifest (byte counts, mixture proportions, sampling seed) this commitment requires"
  C9_cost_matching:
    primary: "TEG_FLOPs"
    deployment: "TEG_Time"
    fairness_check: "raw-byte-matched"
    diagnostic_only: "token-matched"
    never: "constant tokens-per-parameter across tokenizers"
  C10_mini_ladder_veto_only:
    authority: "non-inferiority veto only — block, never rank"
    finality_rule: "final only if (a) directly tokenizer-structural, or (b) persists across both mini-ladder sizes"

five_way_census_taxonomy:
  1_padding: "alignment-only; never semantic; absent from serialized raw-token stream"
  2_active_specials: "BOS/EOS/document separators etc.; legitimately exercised; merge-opaque"
  3_dormant_interface: "T-cell; fixed semantics, merge-opaque, masked until activation"
  4_dormant_scale: "T-scale; fixed semantics, masked until activation"
  5_accidental_dead: "expected to participate but absent/undertrained (count < N); ONLY this category counts against a candidate"

msi_classes:
  1_identical: "genuine id-level copy"
  2_losslessly_canonicalizable: "frozen, context-free deterministic transducer maps every framed encoding to one canonical operand sequence"
  3_context_entangled: "merges obscure operand boundaries or make conversion ambiguous — fatal"

open_pins:
  delta_switch: null        # PIN: not yet decided — G2 switching-cost margin on TEG_FLOPs LCB
  Delta_branch_b: null      # PIN: not yet decided — Branch B non-inferiority margin vs best subword BPB
  epsilon_match: null       # PIN: not yet decided — FLOPs/byte-axis disagreement fork tolerance (C9)
  delta_D1: null            # PIN: not yet decided — D1 addressing-rank non-inferiority margin
  D2_window_W: 2            # default per doc (±2); confirm before TOK-3
  D2_pass_gap_pp: 1         # default per doc (1pp); confirm before TOK-3
  MSI_canonical_min: null   # PIN: not yet decided — PROVISIONAL floor, pending shadow-lane validation at TOK-2c
  census_N: null            # PIN: not yet decided — accidental-dead undertrained-count threshold
  census_F_max: null        # PIN: not yet decided — T-core fertility threshold reject
  census_R_max: null        # PIN: not yet decided — category-5 fraction threshold reject
  teg_min_baseline_checkpoints: null   # PIN: not yet decided
  teg_E_bound_policy: "constrained via extended incumbent baseline run"  # mechanism pinned, numeric bound TBD
  teg_max_extrapolation_fraction: null # PIN: not yet decided
  teg_ci_level: 0.90        # default per doc
  mini_ladder_sizes: [null, null]  # PIN: not yet decided — two sizes, both well below 115M
  tok4_candidate_cap: null  # PIN: not yet decided

incumbent_ledger:
  vocab_total: 71261     # confirmed against ../../v11/artifacts/v11.vocab.json (71260 pieces + special ids)
  vocab_exercised: 17158  # per design doc; NOT independently re-derived here (needs C2 atlas stream)
  tied_params_at_d512: "~36M (~1/3 of a 115M active budget)"
  category_5_unmasked_estimate: "~54k rows"
  fails_C7: true  # no dormant map
  C6_status: "unaudited"
  g0_msi_profile: "carries 3 documented sharp-end incidents: CN-7 day-1 mismatch (NLL 18.0 vs 0.66), CN-10 space-piece lens bug, atlas raw-id/text-query undercount"
  newly_discovered_2026_07_19: >
    4th issue found by this harness's real roundtrip check (bench/tokenizer_bench.py roundtrip,
    driving the compiled v11 binary): 32/32 files in v11/corpus fail exact UTF-8
    round-trip. Root cause at v11-core/src/pretokenize.rs::metaspace()/reverse_metaspace() --
    every whitespace run (space/tab/newline, any length) collapses to one WORD_START marker on
    encode, and decode always reconstructs it as a literal ASCII space, never '\n' or '\t';
    trailing whitespace is dropped entirely. Every newline in a document decodes as a plain
    space; this is a hard reject under the doc's own §2.3 round-trip gate. Not yet reflected in
    the "3 documented incidents" count above -- reported here pending Chris folding it in.
  newly_discovered_2026_07_19_b: >
    Originally logged as a separate "5th issue" (a Python/Rust parity gap: 114 vs 115 tokens,
    0 vs 1 UNK on chromosome.txt) -- now ROOT-CAUSED and confirmed to be the SAME bug as
    newly_discovered_2026_07_19 above, not a distinct issue. Verified directly against the
    real `tokenizers` library: HF's Metaspace pre-tokenizer splits chunks ONLY on the literal
    ASCII space character (0x20) -- tab/newline/CR are NOT split points and stay embedded
    verbatim inside their chunk (confirmed: `Metaspace(...).pre_tokenize_str('a\tb\nc\rd  e')`
    -> `[('▁a\tb\nc\rd', ...), ('▁', ...), ('▁e', ...)]`). v11-core's own reimplementation
    (`pretokenize_chunks()` in tokenizer.rs, used by the real encode path, and
    `metaspace()`/`reverse_metaspace()` in pretokenize.rs) instead treats ANY
    `char::is_ascii_whitespace()` as a chunk boundary -- one divergence, two symptoms: decode
    collapses all non-space whitespace to a literal space (the original finding), and encode
    silently drops/mis-chunks non-space whitespace (chromosome.txt's single trailing '\n' is
    dropped as a delimiter by v11-core -> 114 tok/0 UNK, but real HF glues it onto the last
    chunk where it has no vocab piece -> 1 extra UNK -> 115 tok/1 UNK, confirmed token-for-
    token). Fix identified but NOT applied (changing the incumbent's production runtime is a
    real decision for Chris, not something to unilaterally patch during screening): both
    functions should split on literal 0x20 only, not is_ascii_whitespace(). Registered as
    result `v11_metaspace_root_cause_confirmed` on RUN-20260718-224711-00385.
  fix_applied_2026_07_19: >
    The metaspace fix above WAS applied (per Chris's instruction "lets patch v11"):
    pretokenize_chunks()/metaspace()/reverse_metaspace() now split on literal space (0x20)
    only. 18/18 v11-core unit tests pass (4 new regression tests), full `cargo test --workspace`
    passes. Rebuilt release binary; chromosome.txt now gives 115 tok/1 UNK via the compiled
    Rust binary, token-for-token identical to real HF tokenizers -- the Rust-vs-tokenizer.json
    parity gap is closed and verified (not just argued). Disclosed side effect: unk_gate_pass
    flips true->false on the 32-file harness sample (0->662 UNK total) because v11's vocab has
    zero pieces for literal tab/newline -- this is the fix correctly REVEALING a real,
    pre-existing vocab coverage gap, not a regression. Round-trip still fails 32/32 files, now
    for the honest reason. Registered as result `v11_metaspace_fix_applied_and_verified` on
    RUN-20260718-224711-00385.
  MAJOR_2026_07_19_three_way_divergence: >
    While verifying the fix above, found something bigger, NOT fixed, flagged for Chris:
    native SentencePiece loading the real, sha-verified v11.model (sha256 4ffbfc87...c1dc8e6,
    matches C2_eval_stream.tokenizer_used_for_this_dump exactly -- this is the actual file
    corpus-atlas's a0_dump_stream.py used to build the already-frozen C2 24M-token eval
    stream) gives 99 tokens/0 UNK on chromosome.txt -- neither the old NOR the newly-patched
    Rust/tokenizer.json pair (114or115/0or1) matches this. Root cause: v11.model's own
    normalizer_spec (read directly from the protobuf) is `name: nmt_nfkc,
    remove_extra_whitespaces: True, add_dummy_prefix: True` -- native SentencePiece NFKC-
    normalizes and collapses EVERY whitespace run before tokenizing. tokenizer.json has
    `normalizer: null` -- this step was dropped entirely at whatever export produced
    tokenizer.json/vocab.bin from v11.model. This is a DISTINCT, EARLIER, LARGER bug than the
    pretokenization-chunking one just fixed (export-time normalizer loss, not a Rust
    reimplementation gap) -- it means the frozen, pinned C2 stream was built with a tokenizer
    that disagrees with the artifacts (tokenizer.json/vocab.bin) this repo's own harness and
    Rust runtime use, a direct tension with C5 ("every artifact carries tokenizer sha256...
    refuses hash-mismatched input"). Deliberately NOT reconciled this session -- reconciling
    means deciding which artifact (v11.model's native SP behavior, or tokenizer.json's
    Metaspace-only behavior) is canonical for TOK-0/C5 purposes, a decision for Chris. Registered
    as result `v11_three_way_implementation_divergence` (verdict fail) on
    RUN-20260718-224711-00385.

tok1_grid_status:
  description: "Real evaluation status of the TOK-1 candidate grid, as of 2026-07-19. See tok-1-intrinsic-screen writeup v4 for full detail and candidates.jsonl (artifact tok-v12-candidates-grid-results:latest) for the raw rows."
  candidates_evaluated: 10   # 9 real trained/constructed + v11_incumbent
  algorithm_families_covered: ["unigram_sp", "bpe_sp", "byte_level_bpe", "pure_byte (Branch B)"]
  algorithm_families_not_yet_covered: []  # design doc's 4th family (if any beyond these) TBD -- not yet cross-checked against doc's full family list
  sentencepiece_vocab_ceiling_on_c8_v0_corpus:
    unigram_max_vocab: 6923
    bpe_max_vocab: 20743
    caveat: "hard ceiling of the 1.9MB v0 C8 corpus (c8_corpus_v0.txt), not a library limitation -- will lift once C8 grows past v0. See result sentencepiece_vocab_ceiling on RUN-20260718-224841-00387."
  branch_b_msi_strict_confirmation: "pure_byte_v0 measures MSI-strict = 1.0 exactly across all 116 frame_battery.jsonl instances -- empirical confirmation of design doc's pre-registered prediction P10'(b). See result branch_b_msi_strict_confirmation."
  round_trip_split: "all SentencePiece candidates (unigram_sp, bpe_sp, every size): round_trip_pass=false, unk_count=144 on v11/corpus. byte_level_bpe and pure_byte: round_trip_pass=true, unk_count=0. A real measured tradeoff, not a bug in either family."
  g1_selection_blocked_on: ["census_F_max", "census_R_max", "MSI_canonical_min"]  # see open_pins above -- g1_selection.select_survivors() is implemented+tested (bench/g1_selection.py, bench/test_g1_selection.py, 12 fixtures passing) and candidates.jsonl is real ready input; harness (tokenizer_bench.py grid-screen) correctly refuses to run with these still null
